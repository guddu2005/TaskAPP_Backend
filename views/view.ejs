<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Task View</title>
   <style>
      body {
         font-family: Arial, sans-serif;
         background-color: #f5f5f5;
         color: #333;
         margin: 0;
         padding: 20px;
         animation: fadeIn 1s ease-in;
      }

      h2 {
         text-align: center;
         animation: slideIn 0.5s ease-in-out;
      }

      p {
         text-align: center;
         animation: slideIn 0.7s ease-in-out;
      }

      a,
      button {
         display: inline-block;
         margin: 10px auto;
         padding: 10px 20px;
         color: #fff;
         background-color: #4CAF50;
         border: none;
         border-radius: 5px;
         text-decoration: none;
         text-align: center;
         transition: background-color 0.3s ease, transform 0.3s ease;
         cursor: pointer;
      }

      a:hover,
      button:hover {
         background-color: #45a049;
         transform: translateY(-3px);
      }

      form {
         text-align: center;
      }

      form button {
         background-color: #f44336;
      }

      form button:hover {
         background-color: #e53935;
      }

      @keyframes fadeIn {
         from {
            opacity: 0;
         }

         to {
            opacity: 1;
         }
      }

      @keyframes slideIn {
         from {
            transform: translateY(-20px);
            opacity: 0;
         }

         to {
            transform: translateY(0);
            opacity: 1;
         }
      }
   </style>
</head>

<body>
   <div style="border: 1px solid black; box-shadow: inset  5px 5px 8px rgba(133, 102, 102, 0.5); border-radius: 15px; width: 550px; padding: 10px; ">
      <h2>
         <%= task.title %>
      </h2>
      <p>
         <%= task.description %>
      </p>

      <div style="text-align: center;">
         <a href="/task/edit/<%= task._id %>">EDIT</a>
         <form action="/task/<%= task._id %>?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit">Delete</button>
         </form>
         <a href="/task">Back to tasks</a>
      </div>
   </div>
</body>

</html>