<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title>Task App</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(to right, #74ebd5, #9face6);
      margin: 0;
      padding: 0;
      color: black;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      position: fixed;
      top: 0;
      background-color: #4ca0af;
      padding: 15px;
      text-align: center;
      width: 100%;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      animation: slideIn 1s ease-out;
    }

    @keyframes slideIn {
      from {
        transform: translateY(-100%);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

  
    nav ul li{
      text-align: center;
    }
    nav ul li a {
      text-decoration: none;
      font-weight: bold;
      transition: color 0.3s ease;
    }

    nav ul li a:hover {
      color: #f1c40f;
    }

    ul {
      padding: 0;
      width: 90%;
    }

    li {
      list-style: none;
      margin: 10px 0;
      padding: 15px;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, background-color 0.3s ease, box-shadow 0.3s ease;
      text-align: left;
      width: 95%;
      animation: fadeIn 0.5s ease-in, bounceIn 1s ease-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    @keyframes bounceIn {
      0% {
        opacity: 0;
        transform: scale(0.5);
      }
      50% {
        opacity: 1;
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
      }
    }

    li:hover {
      transform: translateY(-5px);
      background-color: #f0f0f0;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    li a {
      text-decoration: none;
      color:blue;
      font-size: 1.2em;
    }

    li p {
      margin: 10px 0 0 0;
      color: #666;
    }
  </style>
</head>

<body>
  <header>
    <h1>My Task App</h1>
    <nav >
      <ul style="display: flex; justify-content: space-around;">
        <li style="margin-left: 250px;"><a href="/task">Home</a></li>
        <li style="margin-left: 250px;"><a href="/task/new">Add Task</a></li>
        <li style="margin-left: 250px;"><a href="/login">Login</a></li>
      </ul>
    </nav>
  </header>
  <ul style="position: relative; top: 40px;">
    <% tasks.forEach(task => { %>
    <li>
      <a href="/task/<%= task._id %>"><%= task.title %></a>
      <p><%= task.description %></p>
    </li>
    <% }) %>
  </ul>
</body>

</html>
